# autoscripter

A basic shell script writer for python/bash

Currently supports common linux shell commands (eg mkdir)

Adding network commands (ssh, curl) soon!

## Install

Simply download the package and run it with

``` (your python binary here) autoscripter.py ```

The program will prompt you through creating your
scripts!